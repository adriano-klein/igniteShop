import {styled} from '..'

export const SuccessHeaderContainer = styled("header", {
  display: "flex",
  justifyContent: "center",
  alignItems: "center",
  margin: '0 auto',

  a: {
    marginBottom: "8rem",
  }
})